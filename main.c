/* c standard includes */
# include <stdio.h>

/* local includes */
#include "main.h"

/* function declarations */


/******************** main.c ********************/
int main(void)
{
    systeem_status_t status = reset;

    while(1)
    {
        while(status == reset){

        }
        while(status == wacht){

        }
        while(status == gereed){

        }
        while(status == start){

        }
        while(status == nood){

        }
    }

	return EXIT_SUCCES;
}
